

## Bài 1: Tính tổng từ 1 đến N 

**Mô tả**
Cho số nguyên không âm `N`. Hãy tính tổng các số tự nhiên từ `1` đến `N` bằng **hàm đệ quy**.

**Input**
Một số nguyên `N` `(0 ≤ N ≤ 10^5)`

**Output**
In ra tổng từ `1` đến `N`.

**Ví dụ**

```
Input
5

Output
15
```

---

## Bài 2: Tính giai thừa của N

**Mô tả**
Cho số nguyên không âm `N`. Hãy tính `N!` bằng **đệ quy**.

**Input**
Một số nguyên `N` `(0 ≤ N ≤ 15)`

**Output**
In ra giá trị `N!`.

**Ví dụ**

```
Input
5

Output
120
```

---

## Bài 3: Tính tổng chữ số của một số

**Mô tả**
Cho số nguyên dương `N`. Hãy tính tổng các chữ số của `N` bằng **hàm đệ quy**.

**Input**
Một số nguyên dương `N` `(1 ≤ N ≤ 10^18)`

**Output**
In ra tổng chữ số của `N`.

**Ví dụ**

```
Input
12345

Output
15
```

---

## Bài 4: In dãy số giảm dần

**Mô tả**
Cho số nguyên dương `N`. Hãy in các số từ `N` về `1` bằng **đệ quy**.

**Input**
Một số nguyên `N` `(1 ≤ N ≤ 10^5)`

**Output**
In ra các số từ `N` đến `1`, mỗi số cách nhau một dấu cách.

**Ví dụ**

```
Input
4

Output
4 3 2 1
```

---

