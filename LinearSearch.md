## **Bài 1: Tìm vị trí đầu tiên**

**Mô tả**
Cho mảng số nguyên gồm `N` phần tử và một số nguyên `X`. Hãy tìm vị trí (chỉ số - index) xuất hiện đầu tiên của `X` trong mảng. Nếu không tìm thấy, in ra `-1`.

**Input**
* Dòng 1: `N` và `X`
* Dòng 2: `N` số nguyên

**Output**
* Vị trí đầu tiên của `X` hoặc `-1`.

**Ví dụ**
```
Input:
5 3
1 2 3 4 3

Output:
2
```

---

## **Bài 2: Đếm số lần xuất hiện**

**Mô tả**
Cho mảng số nguyên gồm `N` phần tử và một số nguyên `X`. Hãy đếm xem `X` xuất hiện bao nhiêu lần trong mảng.

**Input**
* Dòng 1: `N` và `X`
* Dòng 2: `N` số nguyên

**Output**
* Số lần xuất hiện của `X`.

**Ví dụ**
```
Input:
6 2
1 2 3 2 4 2

Output:
3
```

---

## **Bài 3: Tìm vị trí cuối cùng**

**Mô tả**
Cho mảng số nguyên gồm `N` phần tử và một số nguyên `X`. Hãy tìm vị trí (index) xuất hiện cuối cùng của `X` trong mảng. Nếu không tìm thấy, in ra `-1`.

**Input**
* Dòng 1: `N` và `X`
* Dòng 2: `N` số nguyên

**Output**
* Vị trí cuối cùng của `X` hoặc `-1`.

**Ví dụ**
```
Input:
5 3
1 3 4 3 2

Output:
3
```

---

## **Bài 4: Tìm số lớn thứ hai**

**Mô tả**
Cho mảng số nguyên gồm `N` phần tử. Hãy tìm giá trị lớn thứ hai trong mảng. Giả sử mảng luôn có ít nhất 2 giá trị khác nhau.

**Input**
* Dòng 1: `N`
* Dòng 2: `N` số nguyên

**Output**
* Giá trị lớn thứ hai.

**Ví dụ**
```
Input:
5
1 5 3 5 4

Output:
4
```

---

## **Bài 5: Kiểm tra sự tồn tại**

**Mô tả**
Cho mảng số nguyên gồm `N` phần tử và số nguyên `X`. In ra `TRUE` nếu `X` xuất hiện ít nhất một lần trong mảng, ngược lại in ra `FALSE`.

**Input**
* Dòng 1: `N` và `X`
* Dòng 2: `N` số nguyên

**Output**
* `TRUE` hoặc `FALSE`.

**Ví dụ**
```
Input:
4 7
1 2 3 4

Output:
FALSE
```
